        $mform->addElement('url', '{{ id }}', get_string('{{ id }}', '{{ langCategory }}'), ['size' => 60], ['usefilepicker' => true]);
        $mform->setType('{{ id }}', PARAM_URL);
{% include 'form/form-common-element.twig' %}