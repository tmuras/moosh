        $mform->addElement('date_selector', '{{ id }}', get_string('{{ id }}', '{{ langCategory }}'), array('optional'=>true));
        $mform->setDefault('{{ id }}', time());
{% include 'form/form-common-element.twig' %}