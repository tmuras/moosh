    $editoroptions = array();
    $mform->addElement('editor', '{{ id }}', get_string('langkey', '{{ langCategory }}'), '', $editoroptions);
    $mform->setType(''{{ id }}', PARAM_CLEANHTML);
{% include 'form-common-element.twig' %}