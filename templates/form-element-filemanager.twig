    $fileoptions = array('mainfile'=>'', 'subdirs'=>1, 'maxbytes'=>-1, 'maxfiles'=>-1, 'accepted_types'=>'*', 'return_types'=> null);
    $mform->addElement('filepicker', '{{ id }}', get_string('langkey', '{{ langCategory }}'), null, $fileoptions);
    $mform->addRule(''{{ id }}', $strrequired, 'required', null, 'client');
    $mform->addHelpButton(''{{ id }}', 'langkey_help', '{{ langCategory }}');
